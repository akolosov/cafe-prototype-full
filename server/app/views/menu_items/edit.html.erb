<h1>Editing menu_item</h1>

<%= render 'form' %>

<div class="btn-group">
 <%= link_to 'Back', menu_items_path(@menu_item.menu), :class => 'btn btn-small ' %>
 <%= link_to 'Show', @menu_item, :class => 'btn btn-small btn-info' if can? :read, @menu_item %>
</div>
