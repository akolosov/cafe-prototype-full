<%= form_for(@menu_item, :class => 'form-inline') do |f| %>
  <% if @menu_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_item.errors.count, "error") %> prohibited this menu_item from being saved:</h2>

      <ul>
      <% @menu_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :menu_id %>

  <div class="field">
    <%= f.label :item %><br />
    <%= f.select :item_id, Item.all.collect {|i| [ "#{i.name} (#{i.description})", i.id ] }, { :include_blank => false }, :class => 'span9' %>
  </div>
  <div class="field">
    <%= f.label :cost %><br />
    <%= f.text_field :cost, :class => 'span9' %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn btn-small btn-primary'  %>
  </div>
<% end %>
