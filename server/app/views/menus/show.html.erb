<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @menu.name %>
</p>

<p>
  <b>Startdate:</b>
  <%= @menu.startdate %>
</p>

<p>
  <b>Enddate:</b>
  <%= @menu.enddate %>
</p>

<div class="btn-group">
 <%= link_to 'Back', menus_path, :class => 'btn btn-small ' %>
 <%= link_to 'Edit', edit_menu_path(@menu), :class => 'btn btn-small btn-primary' if can? :update, @menu %>
</div>
