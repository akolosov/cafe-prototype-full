[<% @order_items.each do |order_item| %>
<%= { :id => order_item.id,
      :order_id => order_item.order_id,
      :menu_item_id => order_item.menu_item_id,
      :name => order_item.menu_item.item.name(params[:locale]),
      :description => order_item.menu_item.item.description(params[:locale]),
      :photo => order_item.menu_item.item.photo,
      :groupname => order_item.menu_item.item.group.name(params[:locale]),
      :groupdescription => order_item.menu_item.item.group.description(params[:locale]),
      :groupphoto => order_item.menu_item.item.group.photo,
      :cost => order_item.menu_item.cost,
      :count => order_item.count,
      :summ => order_item.menu_item.cost*order_item.count }.to_json.html_safe %>,
<% end %>{}]