<h1>Editing order</h1>

<%= render 'form' %>

<div class="btn-group">
 <%= link_to 'Back', orders_path, :class => 'btn btn-small ' %>
 <%= link_to 'Show', order_path(@order), :class => 'btn btn-small btn-info' if can? :read, @order %>
</div>