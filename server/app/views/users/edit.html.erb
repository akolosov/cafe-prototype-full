<h1>Editing user</h1>

<%= render 'form' %>

<div class="btn-group">
 <%= link_to 'Back', users_path, :class => 'btn' %>
 <%= link_to 'Show', @user, :class => 'btn btn-small btn-primary' if can? :read, @user %>
</div>